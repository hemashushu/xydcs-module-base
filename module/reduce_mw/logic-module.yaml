title: Reduce (Multiple Wires)
title[zh_CN]: 缩减器（多线路）
description: Parallel connection of multiple wires to form a single wire. Used to implement multiple wire inputs.
description[zh_CN]: 并联多条线路形成一条线路，用于实现多线路输入。
iconFilename: icon.png
group: "Wire"
defaultParameters:
  inputPinCount:
    description: The amount of input pins
    value: 2
    valueType: range
    valueRange:
      from: 2
      to: 16
  bitWidth:
    description: The bit width of each pins
    value: 1
    valueType: range
    valueRange:
      from: 1
      to: 32
